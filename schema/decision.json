{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gmem.dev/schema/decision.json",
  "title": "Decision",
  "description": "An architectural choice with rationale.",
  "type": "object",
  "required": ["title", "decision", "rationale", "date"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Generated server-side if omitted on write"
    },
    "title": { "type": "string", "minLength": 1 },
    "decision": { "type": "string", "minLength": 1, "description": "One-sentence summary of the choice" },
    "alternatives": {
      "type": "array",
      "items": { "type": "string" }
    },
    "rationale": { "type": "string", "minLength": 1 },
    "date": { "type": "string", "format": "date-time" },
    "author": {
      "type": "string",
      "description": "Base58 Solana public key of the developer who recorded the decision. Auto-attributed by gmem when a Solana CLI keypair is configured (v0.4+).",
      "pattern": "^[1-9A-HJ-NP-Za-km-z]{32,44}$"
    },
    "authorCluster": {
      "type": "string",
      "description": "Cluster the author's keypair is configured for at the moment of write. Auto-captured alongside `author`.",
      "enum": ["mainnet-beta", "devnet", "testnet", "localnet", "custom"]
    },
    "relatedTo": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["kind", "id"],
        "additionalProperties": false,
        "properties": {
          "kind": { "type": "string", "enum": ["Program", "Account", "Instruction", "Decision", "Finding", "Integration"] },
          "id":   { "type": "string" }
        }
      }
    }
  }
}
